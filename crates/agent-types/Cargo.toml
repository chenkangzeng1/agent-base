[package]
name = "agent-types"
version = "0.1.0"
edition = "2024"
description = "Pure type definitions for the agent-base runtime"
license = "MIT"
repository = "https://github.com/hibuka-labs/agent-base"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
