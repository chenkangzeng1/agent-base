/target
.env
.DS_Store
agent-works.md
harness.md
implrove-from-codex.md
.idea/
.claude/
.omo/
docs/
proptest-regressions/

# Release tooling state: run logs and step progress markers. Without these,
# `cargo package` (no include/exclude in Cargo.toml) pulls deploy/logs/*.log
# into the published crate tarball.
deploy/
